(function (prambanan) {
    var _pow;
    _pow = prambanan.helpers.pow;
    a * 1;
    a / 1;
    a.__mod__(1);
    _pow(a, 1);
    "%s %s".__mod__(b, c);
    a > 1;
    a < 2;
    prambanan.exports('', {});
})(prambanan);